# this tokenizer has to be used in python 3.x 
# and need to use the allennlp library
# sorry for that because allennlp seems to not support python 2 but the matchlstm implementation we used was written in python 2

